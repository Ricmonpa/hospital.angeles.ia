[project]
name = "hospital-angeles-ia-web"
version = "1.1.0"
description = "Hospital Ángeles IA — suite web Flask para médicos (consulta, fiscal, seguros, legal)"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "google-genai>=1.46.0",
]

[tool.uv]
dev-dependencies = []
